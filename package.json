{
  "name": "flight-comparison",
  "version": "1.0.0",
  "description": "Flight comparison platform with frontend and backend",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm run dev\"",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "keywords": [
    "flight",
    "comparison",
    "booking",
    "travel"
  ],
  "author": "Ankit Rathore",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
